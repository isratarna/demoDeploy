# --- Database (native MySQL Server, local or VPS) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=changeme
DB_NAME=cicd_learning

# --- Backend ---
PORT=4056

# --- Frontend (must be prefixed NEXT_PUBLIC_ to reach the browser) ---
NEXT_PUBLIC_API_URL=http://localhost:4056
